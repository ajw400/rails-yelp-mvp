<h1>Restaurants Menu</h1>
<p><%= link_to "Add a new restaurant", new_restaurant_path, class: "btn btn-primary" %></p>
<ul>
<% @restaurants.each do |restaurant| %>
  <li><p><%= restaurant.name %></p></li>
  <%= link_to "See details", restaurant_path(restaurant) %>
<% end %>
</ul>
